# Adaptive-Difficulty & Completion-State Logic Checklist: Adaptive Difficulty Quiz

**Purpose**: Formal pre-`/speckit-implement` reviewer gate focused on the requirements quality of this feature's algorithmically trickiest area -- the streak-based difficulty rule, round-robin ordering, completion/`ended_early` state transitions, and bound-holding (FR-002/003/005/007/008, Key Entities) -- plus consistency between spec.md and the design artifacts (plan.md/research.md/data-model.md/contracts/api.md) that resolved several of these decisions.
**Created**: 2026-08-18
**Feature**: [spec.md](../spec.md)

**Note**: This custom checklist is generated by the `/speckit-checklist` command based on feature context and requirements.
**Review Ownership**: This checklist is a reviewer-owned requirements-quality review artifact. Mark an item `[x]` only when the reviewer determines the requirements-quality criterion is satisfied.
**Marker Semantics**: `[x]` means the criterion has been reviewed and satisfied for requirements quality. It does not mean implementation work is complete.

## Requirement Completeness

- [x] CHK001 Is the starting/initial difficulty band for a quiz's first question per topic explicitly specified in spec.md itself, or only inferable by analogy to placement's "unknown → easy" convention? [Gap, Spec §FR-002]
  - Resolved 2026-08-18: FR-002 now states the first question per topic is always requested at `easy`.
- [x] CHK002 Are quiz-start validation requirements (an acceptable `question_count` range, whether chosen topics must all belong to one subject, whether duplicate topic selections are allowed) specified in spec.md, or only introduced later in contracts/api.md? [Gap, Spec §FR-001]
  - Resolved 2026-08-18: FR-001 now states all three (1-50 question count, same-subject topics, no duplicate topic ids) directly; contracts/api.md and data-model.md updated to match.
- [x] CHK003 Are resumption requirements defined for a learner returning to an `in_progress` quiz they previously left (resume where they stopped vs. must start a new quiz)? [Gap, Coverage, Spec Edge Cases]
  - Resolved 2026-08-18: new Edge Cases bullet explicitly scopes resumption out of this milestone (no UI to rediscover a lost `quiz_session_id`; the API itself doesn't prevent it).

## Requirement Clarity

- [x] CHK004 Is "score" in FR-005/SC-001 quantified (raw correct/total count, percentage, or difficulty-weighted) precisely enough that two independent implementations would compute the same number? [Clarity, Spec §FR-005]
  - Resolved 2026-08-18: FR-005 now defines score as the raw correct/total count, not difficulty-weighted.
- [x] CHK005 Does FR-007's "the content artifact's defined bounds" specify whether that means the platform-wide easy/medium/hard scale uniformly, or a possibly narrower per-topic-configured range -- this distinction is currently resolved only in a plan-level research.md assumption, not in the requirement itself? [Ambiguity, Spec §FR-007]
  - Resolved 2026-08-18: FR-007 now states explicitly that bounds are the platform-wide scale, not per-topic-configurable.
- [x] CHK006 Is the streak-reset behavior specified for the case where two consecutive same-direction answers would move the band but it is already held at a bound -- does the streak reset to zero even though the band didn't change, or only when the band actually changes? [Ambiguity, Spec §FR-002, §FR-007]
  - Resolved 2026-08-18: FR-002 now states the streak resets whenever the two-answer threshold is reached, whether or not the band actually moved.

## Requirement Consistency

- [x] CHK007 Does FR-002's per-topic streak rule (Clarifications) read consistently with FR-007's bound-holding requirement -- i.e. is it unambiguous that "holding at the bound" plus "the streak counter resets... every time the band changes" together fully describe the held-at-bound case? [Consistency, Spec §FR-002, §FR-007]
  - Resolved 2026-08-18: now consistent as a direct consequence of the CHK006 fix -- FR-002 no longer relies on "band changes" alone to describe the reset trigger.
- [x] CHK008 Is `QuizSession`'s three-value status enum (`in_progress`/`completed`/`ended_early`) referenced consistently across the Key Entities section, FR-005, FR-008, and the Edge Cases section, with no leftover reference to a fourth "abandoned" status anywhere? [Consistency, Spec §Key Entities]
  - Verified 2026-08-18, no change needed: every "abandoned" occurrence in spec.md is plain-English description of learner behavior, never used as a status-enum value.

## Acceptance Criteria Quality

- [x] CHK009 Can SC-001's "identical...across ten repeated runs" be objectively verified without also asserting identical question *text*, given question generation itself is LLM-based and not claimed to be deterministic? [Measurability, Spec §SC-001]
  - Resolved 2026-08-18: SC-001 now explicitly scopes the determinism claim to difficulty progression and score, not question text.
- [x] CHK010 Does SC-002's "100% of questions... verified by an automated check comparing quiz-session data against persisted mastery state" specify what counts as a passing comparison (e.g. exact `update_count` match) precisely enough to implement the check without further interpretation? [Measurability, Spec §SC-002]
  - Resolved 2026-08-18: SC-002 now states the passing criterion (`update_count` increment and posterior `p_mastery` match exactly).
- [x] CHK011 Is SC-004's "zero near-duplicate questions" measurable against a stated similarity criterion, or does it rely entirely on an external cross-reference (FR-008 → Milestone 1's rule) that a reader must already know to look up? [Traceability, Spec §SC-004]
  - Resolved 2026-08-18: SC-004 now cross-references FR-008/Milestone 1's similarity criteria directly rather than leaving the reader to infer the connection.

## Scenario Coverage

- [x] CHK012 Is it specified whether one topic's near-duplication retry exhaustion ends the *entire* multi-topic quiz session, versus only removing that topic from the round-robin rotation while the quiz continues on its remaining topics? [Coverage, Spec Edge Cases]
  - Resolved 2026-08-18: Edge Cases bullet now states explicitly that the whole session ends, with rationale (surfacing the depth-mismatch to the learner rather than silently reshuffling).
- [x] CHK013 Are requirements defined for what a learner sees/receives when a quiz ends via `ended_early` rather than normal completion -- is a partial score/summary still produced the same way FR-005 describes for a normal completion? [Gap, Coverage, Spec §FR-005 vs §FR-008]
  - Resolved 2026-08-18: FR-005 now states an `ended_early` quiz still produces a score/summary in the same shape, covering whatever was answered.
- [x] CHK014 Are concurrent-request scenarios addressed -- e.g. two requests both trying to answer the same in-quiz question, or generate the next question, before the prior response is recorded? [Gap, Coverage, Exception Flow]
  - Resolved 2026-08-18: new Edge Cases bullet documents that answering inherits the existing conflict-error guarantee, and that racing next-question requests are an accepted limitation matching Milestone 1's existing scope, not a new gap.

## Edge Case Coverage

- [x] CHK015 Is the behavior specified for a quiz where `question_count` is smaller than the number of chosen topics (e.g. 2 topics but `question_count: 1`) -- does round-robin still start at `topic_ids[0]` with the quiz simply never reaching the later topics, and is that acceptable? [Gap, Edge Case, Spec §FR-001]
  - Resolved 2026-08-18: FR-001 now states this explicitly as expected behavior, not an error.
- [x] CHK016 Is the maximum `question_count` a learner may request bounded anywhere in the requirements, or is an arbitrarily large value implicitly in scope per FR-001 as written? [Gap, Edge Case, Spec §FR-001]
  - Resolved 2026-08-18: FR-001 now bounds `question_count` to 1-50 inclusive, with rationale recorded.

## Non-Functional Requirements

- [x] CHK017 Does FR-009's logging requirement specify what must be captured for a *held-at-bound* difficulty decision (that a bound was hit, not just the resulting band), so "why was this question this difficulty" remains answerable per Constitution Principle V even in that case? [Gap, Spec §FR-009]
  - Resolved 2026-08-18: FR-009 now requires the logged record capture whether a bound was held, matching data-model.md's existing `held_at_bound` payload field.

## Dependencies & Assumptions

- [x] CHK018 Is the Assumptions section's claim that in-quiz difficulty adjustment is "a new session/interaction pattern layered on the existing agents' responsibilities" (not a new agent) validated against Constitution Principle IV's actual bar (distinct responsibility + own evaluation criteria), or merely asserted? [Assumption, Spec §Assumptions]
  - Resolved 2026-08-18: Assumptions section now argues the point concretely against Principle IV's actual bar (no model call, no independent evaluation surface) rather than asserting the conclusion alone.

## Ambiguities & Conflicts

- [x] CHK019 Is there any remaining ambiguity in how "topic-relative" difficulty (Edge Cases) interacts with a learner selecting a topic more than once, or is `topic_ids` guaranteed to already be a set of distinct, validated topic identifiers by the time this requirement applies? [Ambiguity, Spec §FR-002, Edge Cases]
  - Resolved 2026-08-18: FR-001's new duplicate-rejection rule guarantees `topic_ids` is a set of distinct ids by the time FR-002 applies.

## Notes

- Mark items `[x]` only after review confirms the requirement-quality criterion is satisfied
- Leave items unchecked when they still require clarification, correction, or reviewer evaluation
- `/speckit-implement` reads checklist checkbox state as a gate and must not modify markers
- `checklists/requirements.md` has a separate built-in lifecycle maintained by `/speckit-specify` and `/speckit-clarify`
- Add comments or findings inline
- Link to relevant resources or documentation
- Items are numbered sequentially for easy reference
- All 19 items resolved 2026-08-18 by amending spec.md directly (FR-001, FR-002, FR-005, FR-007, FR-009, SC-001, SC-002, SC-004, Edge Cases, Assumptions), with plan.md/data-model.md/contracts/api.md updated to match where the fix introduced a new spec-level constraint (the 1-50 `question_count` bound)
